# `Dim2` и `Dim2F`
```protobuf
syntax = "proto3";

package xproto.pb;

/// 2D vector of floats
message Dim2F {
  /// X component
  float x = 1;
  
  /// Y component
  float y = 2;
}

/// 2D vector of ints
message Dim2 {
  /// X component
  int32 x = 1;
  
  /// Y component
  int32 y = 2;
}
```

Представляет собой двумерный вектор для *целых* чисел и чисел *с плавающей точкой* соответственно.

|      | Доступен | Представлен в виде                                   |
| ---- | -------- | ---------------------------------------------------- |
| C++  | ✅        | [QVector2D](https://doc.qt.io/qt-6/qvector2d.html)   |
| Rust | ✅        | Не преобразуется                                     |
| QML  | ✅        | [vector2d](https://doc.qt.io/qt-6/qml-vector2d.html) |

# `Dim3` и `Dim3F`
```protobuf
syntax = "proto3";

package xproto.pb;

/// 3D vector of floats
message Dim3F {
  /// X component
  float x = 1;

  /// Y component
  float y = 2;

  /// Z component
  float z = 3;
}

/// 3D vector of ints
message Dim3 {
  /// X component
  int32 x = 1;

  /// Y component
  int32 y = 2;

  /// Z component
  int32 z = 3;
}
```

Представляет собой трехмерный вектор для *целых* чисел и чисел *с плавающей точкой* соответственно.

|      | Доступен | Представлен в виде                                   |
| ---- | -------- | ---------------------------------------------------- |
| C++  | ✅        | [QVector3D](https://doc.qt.io/qt-6/qvector3d.html)   |
| Rust | ✅        | Не преобразуется                                     |
| QML  | ✅        | [vector3d](https://doc.qt.io/qt-6/qml-vector3d.html) |
# `EulerAngles`
```protobuf
syntax = "proto3";

package xproto.pb;

/// Euler angles
message EulerAngles {
  /// Roll component in radians
  float roll = 1;
  
  /// Pitch component in radians
  float pitch = 2;
  
  /// Yaw component in radians
  float yaw = 3;
}
```

Описывает поворот объекта в трёхмерном пространстве ([Википедия: Углы Эйлера](https://ru.wikipedia.org/wiki/%D0%A3%D0%B3%D0%BB%D1%8B_%D0%AD%D0%B9%D0%BB%D0%B5%D1%80%D0%B0)).
Значения поворотов выражены в радианах.

|      | Доступен | Представлен в виде                |
| ---- | -------- | --------------------------------- |
| C++  | ✅        | Не преобразуется                  |
| Rust | ✅        | Не преобразуется                  |
| QML  | ❌        | Представляется разбиением на поля |

# `LatLon`
```protobuf
syntax = "proto3";

package xproto.pb;

message LatLon {
  /// WGS84 latitude in degrees
  double latitude = 1;
 
  /// WGS84 longitude in degrees
  double longitude = 2;
}
```

Описывает геопозицию объекта в виде широты и долготы. 
Значения выражены в градусах в соответствии с проекцией WGS84.

|      | Доступен | Представлен в виде                                                               | Примечание                                                                                |
| ---- | -------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| C++  | ✅        | [QGeoCoordinate](https://doc.qt.io/qt-6/qgeocoordinate.html)                     | Поле `altitude` не заполняется. Оно может быть в дальнейшем в ходе других преобразований. |
| Rust | ✅        | Не преобразуется                                                                 |                                                                                           |
| QML  | ✅        | [\[QtPositioning.\]geoCoordinate](https://doc.qt.io/qt-6/qml-geocoordinate.html) | Поле `altitude` не заполняется. Оно может быть в дальнейшем в ходе других преобразований. |
# `UUID`
```protobuf
syntax = "proto3";

package xproto.pb;

/// UUID based on RFC 4122
message UUID {
  /// The string representation of the UUID
  bytes value = 1;
  
  /// The version of the UUID. Can be zero.
  uint32 version = 2;
}
```

Универсальный уникальный идентификатор ([Википедия: UUID](https://ru.wikipedia.org/wiki/UUID)). 
Представлен в виде строки (в формате `123e4567-e89b-12d3-a456-426655440000`). Поле `version` заполняется опционально.

|      | Доступен | Представлен в виде                                              | Примечание                                                                                                 |
| ---- | -------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| C++  | ✅        | `rll::uuid`                                                     |                                                                                                            |
| Rust | ✅        | [uuid::Uuid](https://docs.rs/uuid/latest/uuid/struct.Uuid.html) |                                                                                                            |
| QML  | 🔀       | [string](https://doc.qt.io/qt-6/qml-string.html)                | Доступно посредством преобразования в `QString` (не выбрасывает ошибок) и обратно (может выбросить ошибку) |
